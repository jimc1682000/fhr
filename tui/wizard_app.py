from typing import Dict, Any


def run_app(prefill: Dict[str, Any]) -> None:
    """Temporary placeholder for the Textual App.

    M1 scope: ensure CLI can hand off without doing any work here.
    Later steps will implement the full wizard.
    """
    # Defer textual import until absolutely needed later
    return

